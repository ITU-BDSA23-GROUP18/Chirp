@page "/{author}"
@model UserTimelineModel

@{
    ViewData["Title"] = "Chirp!";
    Layout = "Shared/_Layout";
    var user = HttpContext.GetRouteValue("author");
}

<div>
    <h2> @user's Timeline </h2>
    <button class="follow/unfollow_btn">@($"{UserTimelineModel.IsFollowing(user!.ToString()!, @User.Identity?.Name!)}")</button>

    @if (Model.Cheeps.Any())
    {
        <ul id="messagelist" class="cheeps">
            @foreach (var cheep in Model.Cheeps)
            {
                <li>
                    <p>
                        <strong>
                            <a href="/@cheep.Author">@cheep.Author</a>
                        </strong>
                        @cheep.Message
                        <small>&mdash; @cheep.Timestamp</small>
                    </p>
                </li>
            }
        </ul>
    }
    else
    {
        <em>There are no cheeps so far.</em>
    }
    
    @{
        await Html.RenderPartialAsync("Shared/PageNumbering", Model.Pagination);
    }
</div>
